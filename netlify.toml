# netlify.toml

[build]
  command = "npm run build"  # The build command for your React project
  publish = "build"  # The directory that should be deployed, usually "build" for React

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
